import messages from './messages.json';
import labels from './labels.json';
import text from './text.json';
import data from './data.json';

export default {...labels, ...messages, ...text, ...data };
